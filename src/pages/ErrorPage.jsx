import UseTitle from "../components/UseTitle";

const ErrorPage = () => {
    UseTitle("Error")
    return (
        <div>
             404 not found.
        </div>
    );
};

export default ErrorPage;